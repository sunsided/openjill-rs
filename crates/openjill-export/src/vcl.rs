//! VCL export stubs.

use openjill_data::vcl::VclFile;

/// Converts parsed `*.VCL` text entries into one export string.
pub fn file_to_string(_file: &VclFile) -> String {
    unimplemented!("VCL export wiring lands in a follow-up issue")
}
