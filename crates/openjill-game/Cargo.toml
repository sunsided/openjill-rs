[package]
name = "openjill-game"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
# Audio mixer and core domain types are wired in upcoming subsystems; kept here to document the runtime graph.
openjill-audio = { path = "../openjill-audio" }
openjill-core = { path = "../openjill-core" }
openjill-data = { path = "../openjill-data" }
openjill-render = { path = "../openjill-render" }
pollster.workspace = true
thiserror.workspace = true
wgpu.workspace = true
winit.workspace = true
